---
import { Icon } from 'astro-icon/components'
import { ThemeToggle } from './react/ThemeToggle.tsx'
---

<nav class="backdrop-saturate-100 backdrop-blur-md flex items-center justify-between w-full max-w-full mx-auto px-64 bg-background-color text-base h-14 box-border fixed top-0 left-0 z-50">
  <h1 class="text-base font-medium m-0 tracking-normal">Tomás Hurrell</h1>
  <div class="flex items-center">
    <a
      target="_blank"
      href="https://www.linkedin.com/in/tomas-hurrell/"
      rel="noopener noreferrer"
    >
      <button
        aria-label="LinkedIn"
        class="flex items-center justify-center w-9 h-9 p-0 mx-2"
        auto
        type="abort"
      >
        <Icon name="mdi:linkedin" size={32} aria-label="LinkedIn" />
      </button>
    </a>
    <a
      target="_blank"
      href="https://github.com/hurrellt"
      rel="noopener noreferrer"
    >
      <button
        aria-label="Github"
        class="flex items-center justify-center w-9 h-9 p-0 mx-2"
        auto
        type="abort"
      >
        <Icon name="mdi:github" size={32} aria-label="Github" />
      </button>
    </a>
    <ThemeToggle client:load />
    <!-- <a target="" href="#theme">
      <button
        aria-label="Toggle Dark mode"
        class="flex items-center justify-center w-9 h-9 p-0 mx-2"
        auto
        type="abort"
        onClick={() => prefers.switchTheme(theme.type === "dark" ? "light" : "dark")}
      >
        {theme.type === "dark" ? (
          <Icon name="mdi:weather-night" size={16} />
        ) : (
          <Icon name="mdi:weather-sunny" size={16} />
        )}
      </button>
    </a> -->
  </div>
</nav>